# Git Industry Level Commands

## 1. Git Configuration Commands

### git config --global user.name

Syntax:
git config --global user.name "Your Name"

Purpose:
Sets the username for Git commits.

Example:
git config --global user.name "Navya"

Screenshot:
(Add screenshot)

---

### git config --global user.email

Syntax:
git config --global user.email "your@email.com"

Purpose:
Sets the email for Git commits.

Example:
git config --global user.email "navya@gmail.com"

Screenshot:
(Add screenshot)

---

### git config --list

Syntax:
git config --list

Purpose:
Shows all Git configuration settings.

Example:
git config --list

Screenshot:
(Add screenshot)

---

### git config --unset

Syntax:
git config --unset user.name

Purpose:
Removes a Git configuration value.

Example:
git config --unset user.name
